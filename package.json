{
  "name": "crvfunder",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ crvfunder",
    "create-local": "graph create --node http://localhost:8020/ crvfunder",
    "remove-local": "graph remove --node http://localhost:8020/ crvfunder",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 crvfunder"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
